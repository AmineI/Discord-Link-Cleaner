name: Build and push tagged Docker image

# Version tags always publish, without a file path filter.
on:
  push:
    tags: ['*']

jobs:
  publish:
    uses: ./.github/workflows/docker-publish.yml
    permissions:
      contents: read
      packages: write